# Product

## Register

product

## Users

`pod ui` 本地控制台的使用者：

- **主要**：出海 OPC / 独立开发者。本机跑 2–5 个 agent（Claude Code、Cursor、OpenClaw、Codex）+ 3–15 个 MCP server。自己就是运维和安全负责人。
- **次要**：2–10 人微型工作室的技术负责人，需要向客户交代"这个 agent 能碰什么、做过什么"。
- **使用情境**：装完 pod、录了几天真实调用之后，打开 `127.0.0.1:8787` 的本地控制台，想知道这台机器上到底有哪些 agent 资产、各自被授予了什么、最近做了什么、哪里最可疑。单人、只读、本地查看；也有投屏或截图给客户看的时刻。
- **要完成的任务**：30 秒内回答四个问题——**有哪些 agent → 它们能碰什么 → 最近做过什么 → 哪里最危险**。

## Product Purpose

`pod ui` 是 pod 的本地 Web 控制台（`127.0.0.1` + token，默认只读）。它把 CLI 已经落盘的策略、审计哈希链、capability graph 聚合成一屏可读的资产视图，第一个页面是 **Agent 资产**（Asset Registry / agent 清单）。

成功的样子：用户第一次打开就明白本机 agent 资产的全貌，不需要读文档、不需要敲 CLI；每个数字都能追到它的出处。

## Brand Personality

克制、可信、可核查（restrained, credible, verifiable）。安全工具的自信来自把证据摆出来，而不是来自形容词。语气像一份被认真维护的台账：安静、精确、不恐吓、不营销腔。术语保留英文原文（agent / server / tool / policy / deny）。

## Anti-references

- **恐吓式安全仪表盘**：满屏红色、"RISK SCORE 87" 大字、无出处的结论。
- **通用 SaaS 后台模板**：KPI 大数字 + 渐变强调 + 堆叠卡片阴影。
- **终端崇拜**：为了"极客感"牺牲可读性（通篇等宽字体、荧光绿文字、纯黑背景）。
- **夸张叙事**：与 `packages/gateway/src/snapshot.ts` 头注释确立的工程纪律一致——"说清楚不夸大"。

## Design Principles

1. **证据优先，结论其次**：每个风险信号都能点回出处（策略行 / 审计条目 / graph path），不出现无来源的判断。
2. **说清楚不夸大**：缺数据就写"未记录"，不用模糊的"安全 / 风险"标签掩盖信息缺口；宁可显示空状态也不编造。
3. **本地优先的信任观感**：界面本身要让用户确信数据没有离开本机——只读默认、来源可见。
4. **面向任务，不面向功能**：信息顺序 = 用户的问题顺序（有哪些 → 能碰什么 → 最近做过什么 → 哪里可疑）。
5. **只读是默认，写操作要显式**：v1 控制台不改变任何状态；策略编辑、审批升级为后续显式动作。

## Accessibility & Inclusion

- WCAG 2.1 AA：正文对比度 ≥ 4.5:1，非文本元素 ≥ 3:1。
- 状态不能只靠颜色区分：三态决策（allow / approve / deny）必须同时有文字或形状标记。
- 支持 `prefers-reduced-motion`。
- 界面语言中文为主，技术术语保留英文原文。
- 键盘可达：所有可交互元素有可见 focus 态。
