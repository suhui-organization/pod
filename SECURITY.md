# Security Policy

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| main 分支（开发版） | 安全更新 |
| 已发布 tags | 安全更新（自发布起 12 个月） |

## 报告漏洞

pod 是安全产品——漏洞报告请直接联系维护者，**不要公开披露**：

- 邮箱：security@podsec.dev（占位，发布前替换）
- 主题请包含 `[pod-security]`，并附上：
  - 受影响版本（git commit / tag）
  - 复现步骤（最小可复现用例）
  - 影响评估（威胁模型中的哪个威胁：T1-T9）

我们承诺：

- 24 小时内确认收到报告
- 7 天内给出初步评估与修复计划
- 修复后按需发布安全公告（GitHub Security Advisory）

## 安全设计基线（与本项目威胁模型一致）

- **审计哈希链**：`packages/audit` 的 SHA-256 链为不可变审计基础，任何篡改必须被 `verify()` 检测。修改该模块必须有对应单测。
- **fail-closed**：策略求值器（`packages/policy`）未授权一律拒绝；审批超时一律拒绝。任何"放宽默认"的改动都需要威胁模型评审。
- **隐私最小化**：审计只存参数哈希；`pod sync` 不上传参数/输出原文。新增传输字段必须走评审。
- **本地优先**：核心功能不依赖云端。云端（Pod Cloud）数据同步需用户显式配置。

## 测试要求

- 安全核心（audit/policy/gateway）新代码必须有单测：`pnpm -r test` 全绿才可合并
- 修改策略求值/哈希链/审批逻辑后，跑完整套件并补充对应用例
